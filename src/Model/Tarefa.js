export default class Tarefa {
    constructor(tarefa, termino, prioridade, status) {
        this.tarefa = tarefa;
        this.termino = termino;
        this.prioridade = prioridade;
        this.status= status;
    }
}